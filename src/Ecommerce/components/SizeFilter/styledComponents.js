import styled from '@emotion/styled'; 
import tw from 'tailwind.macro';


const DisplaySizes=styled.div`${tw `w-700 `}`;

const Button = styled.button `${tw `h-10 w-10 bg-blue-200 rounded-full m-2`}`

export {DisplaySizes,Button};
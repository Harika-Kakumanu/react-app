import styled from '@emotion/styled';
import tw from 'tailwind.macro';

const ProductsHeader=styled.div `${tw `flex justify-between`}`;

const ProductsCount=styled.p `${tw `text-xl`}`;



export {ProductsHeader,ProductsCount};
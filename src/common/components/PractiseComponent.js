import React from 'react';

class PractiseComponent extends React.Component{
    render(){
        return(
            <div>Events that occur when the mouse interacts with the HTML
            document belongs to the MouseEvent Object.
            HTML DOM events allow JavaScript to register different event handlers on elements in an HTML document.
            Events are normally used in combination with functions, and the function will not be executed before
            the event occurs (such as when a user clicks a button).</div>
            );
    }
}
export {PractiseComponent};
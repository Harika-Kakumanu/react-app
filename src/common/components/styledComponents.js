import styled from '@emotion/styled';
import tw from 'tailwind.macro';

const MainContainer=styled.div`
    ${tw `bg-gray-300 flex flex-col justify-center items-center p-6 `}`;
 
const TitleComponent=styled.div `${tw `text-2xl font-bold`}`;


    
export {MainContainer,TitleComponent}
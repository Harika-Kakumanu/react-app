export const add=(a,b)=>{
    if(typeof a==='string' || typeof y==='string'){
        return null;
    }
    return a+b;
};

export const sample=(b)=>{
    if(b){
        return ++b;
    }
};

export const greater=(a,b,c)=>{
   return ((a>b)&&(a>c))? a :((b>c)? b : c);
};

// export const objects=()=>{
//     return
// }